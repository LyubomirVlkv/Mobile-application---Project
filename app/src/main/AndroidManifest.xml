<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalApplication">
        <activity
            android:name=".BMIGraph"
            android:exported="false" />
        <activity
            android:name=".UpdateDelete"
            android:exported="false"
            android:label="@string/title_activity_update_delete"
            android:theme="@style/Theme.FinalApplication.NoActionBar" />
        <activity
            android:name=".DBActivity"
            android:exported="false" />
        <activity
            android:name=".List_view"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>